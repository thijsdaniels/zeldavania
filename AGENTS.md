# Way of Working

Prefer native tool calling for reading and writing files over writing scripts to do that for you. If you do need to write custom scripts to perform complex operations, that's fine, but place them on your scratchpad (`<appDataDir>/scratch/`), not in the project directory.